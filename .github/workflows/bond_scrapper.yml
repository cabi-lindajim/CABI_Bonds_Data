name: Ejecutar BondScrapper semanalmente

on:
  schedule:
    # Ejecutar los viernes a las 13:30 hora de Guatemala (19:30 UTC)
    - cron: '30 19 * * 5'
  workflow_dispatch:

jobs:
  ejecutar-notebook:
    runs-on: ubuntu-latest

    steps:
    - name: Clonar el repositorio
      uses: actions/checkout@v3
      with:
        persist-credentials: true  

    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Instalar dependencias y Jupyter
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
        pip install jupyter papermill

    - name: Ejecutar notebook BondScrapper.ipynb
      run: |
        papermill BondScrapper.ipynb salida.ipynb

    - name: Confirmar y hacer commit de nuevos archivos .csv
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add Input/**/*.csv
        if git diff --cached --quiet; then
          echo "No hay cambios para commitear."
        else
          git commit -m "📊 Nuevos datos de bonos - $(date +'%Y-%m-%d')"
          git push
        fi
